const name = 'Jane';
const Name = 'Mark';
const NAME = 'Fred';

// all separate values

let newvalue = `${value1} ${value2}`;

// self-descriptive
let fullName = `${firstName} ${lastName}`;

let isModalVisible = false;
// tells us it contains a boolean

if (isModalVisible) {
  // do something
}

let isLoading;
let hasPosition;

// constant
const COLOR_RED = '#f00';
